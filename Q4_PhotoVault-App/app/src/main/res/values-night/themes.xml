<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.PhotoVault" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/primary_teal</item>
        <item name="colorPrimaryContainer">@color/primary_teal_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSurface">@color/background_dark</item>
        <item name="android:colorBackground">@color/background_dark</item>
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.PhotoVault.AlertDialog</item>
    </style>

    <style name="Theme.PhotoVault" parent="Base.Theme.PhotoVault" />

    <style name="ThemeOverlay.PhotoVault.AlertDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog">
        <item name="colorPrimary">@color/primary_teal</item>
        <item name="colorError">@color/destructive_red</item>
    </style>
</resources>
